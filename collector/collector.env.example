# /etc/monomi/collector.env
# Copy to that path on the host and chmod 600 — it holds the bearer token.

INGEST_URL=https://monomi.example.workers.dev/ingest
INGEST_TOKEN=replace-me-with-a-long-random-string
INTERVAL=5.0
PIRONMAN_URL=http://127.0.0.1:34001/api/v1.0/get-data
NUT_UPS=eaton3s
